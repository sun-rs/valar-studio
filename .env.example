# ============================================================================
#  Valar Web - Unified Environment Configuration (copy to .env before running)
# ============================================================================
# All services read from this file: backend (FastAPI) and frontend (Vite).
# Replace the placeholder values (especially SECRET_KEY / admin password)!

# ------------------------------------------------------------------
# Application basics
# ------------------------------------------------------------------
APP_NAME="Valar Web"
APP_VERSION="1.0.0"
DEBUG=false

# ------------------------------------------------------------------
# Backend server
# ------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000
DATABASE_URL=sqlite:///./data/valar.db

# ------------------------------------------------------------------
# Security (!!! change in production)
# ------------------------------------------------------------------
SECRET_KEY=change-me-use-openssl-rand-base64-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=change-me-now

# ------------------------------------------------------------------
# Frontend / CORS shared origins (comma separated full origins)
# Hostnames will be extracted automatically for Vite allowedHosts.
# ------------------------------------------------------------------
APP_ALLOWED_ORIGINS=http://localhost:3001,http://localhost:5173

# ------------------------------------------------------------------
# Optional overrides (uncomment as needed)
# ------------------------------------------------------------------
# VITE_PORT=3001
# VITE_HOST=0.0.0.0
# VITE_API_TARGET=http://127.0.0.1:8000

# Additional backend integrations
VALAR_MONGO_CONNECTION=CLOUD

# Application
APP_NAME="Valar Web"
APP_VERSION="1.0.0"
DEBUG=False

# Server
HOST=0.0.0.0
PORT=8000

# Database
DATABASE_URL=sqlite:///./data/valar.db

# Security (IMPORTANT: Generate your own secure values!)
SECRET_KEY=your-secret-key-change-this-immediately
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS (Update with your production domain)
CORS_ORIGINS=["https://yourdomain.com"]

# Admin (IMPORTANT: Change these credentials!)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=change-this-secure-password

# MongoDB Connection (for Valar data)
VALAR_MONGO_CONNECTION=CLOUD